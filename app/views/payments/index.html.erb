<h1>Listing payments</h1>

<%= IO.popen("openssl prime hex") %>

<table>
  <tr>
    <th>Mir</th>
    <th>Invoice no</th>
    <th>Status</th>
    <th>Message</th>
    <th>Return code</th>
    <th>Amount</th>
    <th>Shipping</th>
    <th>Tax1</th>
    <th>Tax2</th>
    <th>Total</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @payments.each do |payment| %>
  <tr>
    <td><%= payment.meant_it_rel_id %></td>
    <td><%= payment.invoice_no %></td>
    <td><%= payment.status %></td>
    <td><%= payment.message %></td>
    <td><%= payment.return_code %></td>
    <td><%= payment.amount %></td>
    <td><%= payment.shipping %></td>
    <td><%= payment.tax1 %></td>
    <td><%= payment.tax2 %></td>
    <td><%= payment.total %></td>
    <td><%= link_to 'Show', payment %></td>
    <td><%= link_to 'Edit', edit_payment_path(payment) %></td>
    <td><%= link_to 'Destroy', payment, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Payment', new_payment_path %>
