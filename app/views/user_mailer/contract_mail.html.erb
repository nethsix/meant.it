Congratulations!

Your order (invoice #:<%= @contract_no %>) for a pii:<%= @likee_pii.pii_value %> (<%= @likee_pii.pii_property_set.short_desc %>) has succeeded!
<% if !@value_type_input_str.nil? %>

Ordered items: <%= @value_type_input_str %>
<% end # end if !@value_type_obj.nil? %>

Please click <a href='/payments/pay/invoice_no/<%= CGI::escape(@contract_no) %>'>here</a> to pay!
