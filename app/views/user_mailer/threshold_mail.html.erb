<% 
  name = nil
  if !@person.nil?
    if !@person.first_name.nil? and !@person.first_name.empty?
      name = @person.first_name
    elsif !@person.last_name.nil? and !@person.last_name.empty? 
      name = @person.last_name
    end # end elsif !@person.last_name.nil? and !@person.last_name.empty? 
  end # end if !@person.nil?
%>
<% if name.nil? %>
Congratulations!<br/>
<% else %>
Congratulations <%= name %>!<br/>
<% end %>

Threshold of <%= ControllerHelper.threshold_display_str_from_pps(@pii.pii_property_set) %> for pii:<%= @pii.pii_value %> (<%= @pii.pii_property_set.short_desc %>) reached!
